{
  "name": "star-collector",
  "version": "1.0.0",
  "description": "A gamified task tracking and reward system for kids",
  "main": "dist/index.js",
  "type": "module",
  "scripts": {
    "build": "node build.js",
    "start": "npx serve dist",
    "dev": "tsc -w & npx serve dist",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "firebase": "^11.5.0"
  },
  "devDependencies": {
    "serve": "^14.2.1",
    "typescript": "^5.3.3"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "browserslist": [
    ">0.2%",
    "not dead",
    "not ie <= 11",
    "not op_mini all"
  ],
  "private": true
}